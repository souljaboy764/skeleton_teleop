{
    "tasks": [
        {
            "type": "shell",
            "label": "catkin_make build active package",
            "command": "catkin_make",
            "args": [
                // "-g",
                // "${file}",
                // "-o",
                // "${fileDirname}/${fileBasenameNoExtension}"
                "--pkg skeleton_teleop"
            ],
            "options": {
                "cwd": "${workspaceFolder}/../.."
            }
        },

        {
            "type": "shell",
            "label": "run active node",
            "command": "rosrun",
            "args": [
                // "-g",
                // "${file}",
                // "-o",
                // "${fileDirname}/${fileBasenameNoExtension}"
                "skeleton_teleop",
                "skeleton_teleop_node"
            ],
            "options": {
                "cwd": "${workspaceFolder}/../.."
            }
        }
    ],
    "version": "2.0.0"
}